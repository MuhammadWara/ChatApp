name: Checkout Workflow

on:
  workflow_call:

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: testing 
        run: echo " chal gaya yaha tk"


        
      
  
